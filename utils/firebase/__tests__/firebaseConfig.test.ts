import { firebase } from '../firebase'

describe('Firebase Config', () => {
    it.todo('Should export firebase config in expected shape')
    it.todo('Should not initialize firebase if already initialized')
})
